<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lesson03.mapper.ReviewMapper">
	<select id="selectReviewById" parameterType="int" resultType="com.example.lesson03.domain.Review">
		SELECT
			`id`,
			`storeId`,
			`menu`,
			`userName`,
			`point`,
			`review`,
			`createdAt`,
			`updatedAt`
		FROM 
		`new_review` 
		WHERE id=#{id};
	</select>
	<select id="insertReview" resultType="com.example.lesson03.domain.Review">
		insert into new_review (`storeId`,
			`menu`,
			`userName`,
			`point`,
			`review`)
			values(
			#{menu}
			,#{userName}
			,#{point}
			,#{review}
			,#{}
			) 
		FROM 
		
		`new_review` 
		WHERE id=#{id};
	</select>
	<insert id="insertReview">
		INSERT INTO new_review (
		    storeId
			,menu
			,userName
			,point
			,review
			,createdAt
			,updatedAt
		)
		VALUES
		(
			#{storeId}
			,#{menu}
			,#{userName}
			,#{point}
			,#{review}
			,now()
			,now()
		) 
	</insert>
	<insert id="insertReviewAsField" parameterType="map">
		INSERT INTO new_review (
		    storeId
			,menu
			,userName
			,point
			,review
			,createdAt
			,updatedAt
		)
		VALUES
		(
			#{storeId}
			,#{menu}
			,#{userName}
			,#{point}
			,#{review}
			,now()
			,now()
		) 
	</insert>
	<update id="updateReviewById" parameterType="map">
		UPDATE new_review 
		SET review=#{review} ,updatedAt=now() 
		where id=#{id}
	</update>
</mapper>